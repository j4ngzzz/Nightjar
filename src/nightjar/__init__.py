"""nightjar — Verified spec-as-source platform for AI-generated code.

Developers write .card.md specs (intent + contracts + tiered invariants).
AI generates code. A 5-stage verification pipeline mathematically proves
the code satisfies the invariants. Code is regenerated from scratch every
build — never manually edited.
"""

__version__ = "0.1.0"
