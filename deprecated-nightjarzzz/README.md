# nightjarzzz → nightjar-verify

This package has been renamed to **[nightjar-verify](https://pypi.org/project/nightjar-verify/)**.

## Migration

```bash
pip uninstall nightjarzzz
pip install nightjar-verify
```

All functionality is now in `nightjar-verify`. This package exists only as a redirect.
